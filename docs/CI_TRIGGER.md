# CI trigger

This file intentionally records the first clean CI trigger after enabling GitHub Actions for the RH Research Engine.

The workflow should run on pushes to `main`, install Python 3.12, perform import smoke tests, and run the full pytest suite.
